# Solidary Food Distribution

To run with `docker` you can run

```sh
docker compose up
```

to get the service up and running.

You then can reach the service under [localhost:8080](http://localhost:8080/)